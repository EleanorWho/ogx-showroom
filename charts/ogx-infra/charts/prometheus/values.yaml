image:
  repository: quay.io/prometheus/prometheus
  tag: v3.12.0
  pullPolicy: IfNotPresent

port: 9090

route:
  enabled: false

persistence:
  size: 5Gi

resources:
  requests:
    memory: "256Mi"
    cpu: "100m"
  limits:
    memory: "512Mi"
    cpu: "500m"
